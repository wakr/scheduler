<%= bootstrap_form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.text_field :name, icon: "tag", placeholder: 'Name of the group'%>
    <%= f.text_area :description , icon:"pencil", placeholder: 'Description of the task'%>
    <%= f.text_field :location , icon:"plane", placeholder: 'Location where task is suppose to be done'%>
    <%= f.hidden_field :assignment_id, :value => @task.assignment_id %>

    <%= f.submit %>
<% end %>
