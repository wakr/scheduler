<div class="panel-body">
  <div class="panel-heading">
<h3>Creator of</h3>
  </div>


<table class="table borderless">


  <tbody>
  <% @user.get_created_assignments.each do |assignment| %>
      <tr>
        <td><%= link_to assignment.name, assignment %></td>
        <% if assignment.is_done %>
            <td><%= link_to 'Destroy', assignment, method: :delete, data: {confirm: 'This item can be removed now. Are you sure?'}, class:'btn btn-danger'%></td>
        <% else %>
            <td></td>
        <%end %>
        <td>
          <% if current_user.id == @user.id %>
            <%= link_to new_user_assignment_path(:assignment_id => assignment.id) do %>
              <i class="fa fa-user-plus btn btn-info">  Add worker</i>
            <% end %>
          <% end %>
        </td>
        <td>
          <%= link_to 'New task', new_task_path(:assignment_id => assignment.id), class:'btn btn-primary' if current_user.id == assignment.creator_id%>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>
</div>